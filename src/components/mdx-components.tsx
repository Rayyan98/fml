import type { MDXComponents } from "mdx/types";

export const mdxComponents: MDXComponents = {
  // All styling is handled via the .prose class in globals.css.
  // Custom component overrides can be added here if needed.
};
